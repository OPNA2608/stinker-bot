-- A Lua file in disguise, you can specify your bot settings here
-- default values are shown in comments (with a -- prefix like this line)

config {
  --the file to log everything in
  --LOGFILE       = "bot.log",
  
  --the quote database file
  --QUOTEFILE     = "quotes.cfg",
  
  --the commands database
  --COMMANDSFILE  = "commands.cfg",
  
  --the files !sbl__size will scan
  --_sizeFILES    = {
  --  "StinkerBot.lua",
  --  "bot.cfg",
  --  "quotes.cfg",
  --  "commands.cfg",
  --},
  
  --display debug messages
  DEBUG           = false,
  
  --the bot's token
  TOKEN           = "Bot <TokenHere>",
  
  --unused, ID of the bot's owner
  --OWNER         = 000000000000000000,
}